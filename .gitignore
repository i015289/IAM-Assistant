# SAP connection credentials — never commit these
.sapcli.env
.sapwiki.env

# Claude — exclude only non-shareable session data
.claude/worktrees/

# macOS
.DS_Store

# Environment — never commit secrets
.env

# Superpowers
.superpowers/
